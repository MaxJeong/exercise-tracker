import React from 'react';
import ReactDOM from 'react-dom';
//import './index.css';
import App from './App';

//App.js returns JSX and loads it into the document with the id named root
ReactDOM.render(<App />, document.getElementById('root'));
